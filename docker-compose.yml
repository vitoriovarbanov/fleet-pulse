services:
  postgres:
    image: postgres:16-alpine
    container_name: fleet-pulse-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fleet_pulse
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: fleet-pulse-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  postgres-prod-copy:
    image: postgres:16-alpine
    container_name: fleet-pulse-db-prod-copy
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fleet_pulse_prod_copy
    ports:
      - "5433:5432"
    volumes:
      - postgres_prod_copy_data:/var/lib/postgresql/data

volumes:
  postgres_data:
  redis_data:
  postgres_prod_copy_data:
